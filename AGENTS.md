# Ash Agent App

This project uses the Ash framework. Before writing code, always read the relevant guide in `node_modules/experimental-ash/dist/docs/public/`.

Task-specific Ash agent guides are available as skills in `node_modules/experimental-ash/dist/skills/`. Use one when it matches the change you are making.
